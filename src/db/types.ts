export type User = {
  id: number;
  google_id: number;
  email: string;
  name: string;
  picture: string;
  access_token: string;
  refresh_token: string;
  scope: string;
  token_type: string;
  id_token: string;
  expiry_date: number;
};
